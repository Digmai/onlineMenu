import styled from "styled-components";

export const MenuWrapper = styled.div`
  max-width: min(425px, 100vh);
  height: 100vh;

  margin: 0 auto;

  overflow: scroll;

  border: rgb(159 159 159 / 12%) 2px solid;
`;
